[package]
name = "zaroxi-core-workspace-history"
version = "0.1.0"
edition = "2024"
description = "File history and snapshot utilities."
license = "MIT"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.

[lints]
workspace = true
