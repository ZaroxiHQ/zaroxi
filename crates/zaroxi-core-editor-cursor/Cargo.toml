[package]
name = "zaroxi-core-editor-cursor"
version = "0.1.0"
edition = "2024"
description = "Cursor primitives and movement semantics."
license = "MIT"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
