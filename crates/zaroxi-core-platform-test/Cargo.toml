[package]
name = "zaroxi-core-platform-test"
version = "0.1.0"
edition = "2024"
description = "Test harness and platform test adapters."
license = "MIT"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
