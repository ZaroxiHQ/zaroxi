[package]
name = "zaroxi-infrastructure-settings"
version = "0.1.0"
edition = "2024"
description = "zaroxi-infrastructure-settings (auto-generated)"
license = "MIT"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
