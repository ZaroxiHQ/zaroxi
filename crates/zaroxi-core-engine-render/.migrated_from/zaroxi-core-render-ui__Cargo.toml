[package]
name = "zaroxi-core-render-ui"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Immediate/retained UI primitives and batching system."
rust-version = "1.85"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
