[package]
name = "zaroxi-core-engine-render"
version = "0.1.0"
edition = "2024"
description = "Rendering coordination for the engine."
license = "MIT"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
