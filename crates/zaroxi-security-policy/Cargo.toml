[package]
name = "zaroxi-security-policy"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Permission language and policy evaluation engine."
rust-version = "1.85"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
