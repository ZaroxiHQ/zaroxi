[package]
name = "zaroxi-platform-syntax"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Language grammars and parser runtime (Tree-sitter integration)."
rust-version = "1.85"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
