[package]
name = "zaroxi-kernel-core"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Kernel core primitives for Zaroxi (zero-dependency): strongly-typed IDs and tiny helpers."
rust-version = "1.85"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
