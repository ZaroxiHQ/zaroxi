[package]
name = "zaroxi-kernel-core"
version = "0.1.0"
edition = "2024"
description = "zaroxi-kernel-core (auto-generated)"
license = "MIT"

[dependencies]
zaroxi-kernel-types = { path = "../zaroxi-kernel-types" }
zaroxi-kernel-errors = { path = "../zaroxi-kernel-errors" }

# Restored explicit workspace-owned dependencies required by ids.rs.
# Use workspace = true so versions/features are centrally controlled.
serde = { workspace = true }
uuid = { workspace = true }

[lints]
workspace = true
