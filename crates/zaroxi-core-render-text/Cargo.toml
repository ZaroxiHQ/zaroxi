[package]
name = "zaroxi-core-render-text"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Glyph shaping, atlas management and GPU text uploading."
rust-version = "1.85"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
