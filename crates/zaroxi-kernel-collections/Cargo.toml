[package]
name = "zaroxi-kernel-collections"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Optimized collections and lock-free containers."
rust-version = "1.85"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
