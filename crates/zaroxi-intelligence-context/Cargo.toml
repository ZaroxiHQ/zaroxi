[package]
name = "zaroxi-intelligence-context"
version = "0.1.0"
edition = "2024"
description = "zaroxi-intelligence-context (auto-generated)"
license = "MIT"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
