[package]
name = "zaroxi-infrastructure-metrics"
version = "0.1.0"
edition = "2024"
description = "zaroxi-infrastructure-metrics (auto-generated)"
license = "MIT"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
