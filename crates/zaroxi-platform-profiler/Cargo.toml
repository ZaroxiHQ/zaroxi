[package]
name = "zaroxi-platform-profiler"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Profiler hooks and integrations."
rust-version = "1.85"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
