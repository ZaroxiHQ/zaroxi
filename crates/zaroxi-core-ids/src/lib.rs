#![doc = "Deprecated compatibility wrapper: re-export identifier types from `zaroxi-foundation`. \
This crate is retained temporarily to avoid breaking dependents while migrations happen."]

pub use zaroxi_foundation::{BufferId, DocumentId, WorkspaceId};
