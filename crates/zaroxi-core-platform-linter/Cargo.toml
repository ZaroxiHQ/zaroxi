[package]
name = "zaroxi-core-platform-linter"
version = "0.1.0"
edition = "2024"
description = "Linter integration adapters."
license = "MIT"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
