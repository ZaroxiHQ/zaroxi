[package]
name = "zaroxi-application-editor"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Application orchestration for editor features (commands → domain → core)."
rust-version = "1.70"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
