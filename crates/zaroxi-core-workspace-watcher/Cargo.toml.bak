[package]
name = "zaroxi-workspace-watcher"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Trait-only watcher interface; platform impl in infra."
rust-version = "1.70"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
