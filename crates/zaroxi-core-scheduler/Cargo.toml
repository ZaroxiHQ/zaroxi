[package]
name = "zaroxi-core-scheduler"
version = "0.1.0"
edition = "2024"
description = "zaroxi-core-scheduler (auto-generated)"
license = "MIT"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.

[lints]
workspace = true
