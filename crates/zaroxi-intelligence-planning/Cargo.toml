[package]
name = "zaroxi-intelligence-planning"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Plan generation and step decomposition algorithms."
rust-version = "1.85"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
