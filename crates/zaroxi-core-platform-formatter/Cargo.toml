[package]
name = "zaroxi-core-platform-formatter"
version = "0.1.0"
edition = "2024"
description = "Formatter adapters and integrations."
license = "MIT"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
