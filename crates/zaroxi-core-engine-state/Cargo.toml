[package]
name = "zaroxi-core-engine-state"
version = "0.1.0"
edition = "2024"
description = "Engine-local state primitives for rendering and UI."
license = "MIT"

[dependencies]

[lints]
workspace = true
