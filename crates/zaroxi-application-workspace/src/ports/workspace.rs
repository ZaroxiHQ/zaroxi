/*!
Focused workspace-related ports (placeholder).

Currently this file is a small placeholder to host later finer-grained workspace-only
contracts (if we want them separated from the types module). For now the primary
public surface continues to come from `crate::ports::types`.
*/
