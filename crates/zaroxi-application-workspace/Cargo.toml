[package]
name = "zaroxi-application-workspace"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Workspace orchestration: open/close, indexing triggers and watchers."
rust-version = "1.85"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
