[package]
name = "zaroxi-application-project"
version = "0.1.0"
edition = "2024"
description = "zaroxi-application-project (auto-generated)"
license = "MIT"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
