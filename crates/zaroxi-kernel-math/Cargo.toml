[package]
name = "zaroxi-kernel-math"
version = "0.1.0"
edition = "2024"
description = "zaroxi-kernel-math (auto-generated)"
license = "MIT"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
