[package]
name = "zaroxi-domain-settings"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Canonical settings model (no IO)."
rust-version = "1.70"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
