[package]
name = "zaroxi-domain-history"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Change history and checkpointing logic."
rust-version = "1.85"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
