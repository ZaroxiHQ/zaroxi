[package]
name = "zaroxi-core-editor-selection"
version = "0.1.0"
edition = "2024"
description = "Selection representation and utilities."
license = "MIT"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.

[lints]
workspace = true
