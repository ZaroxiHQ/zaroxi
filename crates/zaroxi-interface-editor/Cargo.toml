[package]
name = "zaroxi-interface-editor"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Concrete editor UI combining core-ui and domain models."
rust-version = "1.85"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
