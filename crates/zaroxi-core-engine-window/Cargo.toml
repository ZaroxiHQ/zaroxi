[package]
name = "zaroxi-core-engine-window"
version = "0.1.0"
edition = "2024"
description = "Thin winit window wrapper for the Zaroxi engine."
license = "MIT"

[dependencies]
winit = { workspace = true }
raw-window-handle = "0.6.2"
