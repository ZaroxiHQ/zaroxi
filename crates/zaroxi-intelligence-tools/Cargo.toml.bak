[package]
name = "zaroxi-intelligence-tools"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Trait-only tool interfaces for agents (filesystem, git)."
rust-version = "1.70"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
