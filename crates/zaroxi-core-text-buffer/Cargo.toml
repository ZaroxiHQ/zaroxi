[package]
name = "zaroxi-core-text-buffer"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "High-level buffer model with snapshots and change history."
rust-version = "1.85"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
