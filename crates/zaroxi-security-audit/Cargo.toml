[package]
name = "zaroxi-security-audit"
version = "0.1.0"
edition = "2024"
description = "zaroxi-security-audit (auto-generated)"
license = "MIT"

[lib]
name = "zaroxi_security_audit"
path = "src/lib.rs"

[dependencies]
serde = { workspace = true }
chrono = { workspace = true }

[features]
default = []

[lints]
workspace = true
