[package]
name = "zaroxi-core-ui"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Low-level widgets and input integration (core UI primitives)."
rust-version = "1.85"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
