[package]
name = "zaroxi-core-commands"
version = "0.1.0"
edition = "2024"
description = "zaroxi-core-commands (auto-generated)"
license = "MIT"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
