[package]
name = "zaroxi-infrastructure-memory"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[dependencies]
zaroxi-kernel-types = { path = "../zaroxi-kernel-types" }
# History/event port implemented by this infra adapter
zaroxi-application-workspace = { path = "../zaroxi-application-workspace" }

# Small durable serialization helpers used by the checkpoint adapter
serde = { workspace = true }
serde_json = { workspace = true }
uuid = { workspace = true }
