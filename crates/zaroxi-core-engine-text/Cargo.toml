[package]
name = "zaroxi-core-engine-text"
version = "0.1.0"
edition = "2024"
description = "Engine text shaping and rendering primitives."
license = "MIT"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
