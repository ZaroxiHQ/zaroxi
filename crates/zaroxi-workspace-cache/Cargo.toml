[package]
name = "zaroxi-workspace-cache"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Caching policy interfaces and in-memory cache impl."
rust-version = "1.85"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
