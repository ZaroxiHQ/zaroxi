[package]
name = "zaroxi-kernel-types"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Kernel types: Id, Position, Span, Range; small serializable primitives."
rust-version = "1.85"
publish = false

[dependencies]
uuid = { workspace = true }
serde = { workspace = true }
