[package]
name = "zaroxi-core-engine-style"
version = "0.1.0"
edition = "2024"
description = "Styling and theming primitives for engine UI."
license = "MIT"

[dependencies]
serde = { workspace = true, features = ["derive"] }

[lints]
workspace = true
