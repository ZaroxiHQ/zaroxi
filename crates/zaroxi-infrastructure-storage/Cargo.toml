[package]
name = "zaroxi-infrastructure-storage"
version = "0.1.0"
edition = "2024"
description = "zaroxi-infrastructure-storage (filesystem adapter)"
license = "MIT"

[dependencies]
# Infrastructure adapter: rely only on the low-level core IO crate to avoid
# pulling in higher-level core-runtime transitive dependencies.
zaroxi-core-io = { path = "../zaroxi-core-io" }
