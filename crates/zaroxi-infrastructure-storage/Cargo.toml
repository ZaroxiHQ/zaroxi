[package]
name = "zaroxi-infrastructure-storage"
version = "0.1.0"
edition = "2024"
description = "zaroxi-infrastructure-storage (filesystem adapter)"
license = "MIT"

[dependencies]
# No direct workspace/core dependencies required for this adapter.
# Keep the crate minimal and self-contained (use std::fs directly).
