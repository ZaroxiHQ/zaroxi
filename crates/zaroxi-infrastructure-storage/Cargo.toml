[package]
name = "zaroxi-infrastructure-storage"
version = "0.1.0"
edition = "2024"
description = "zaroxi-infrastructure-storage (filesystem adapter)"
license = "MIT"

[dependencies]
# Adapter depends on the workspace file ports crate.
zaroxi-core-workspace-files = { path = "../zaroxi-core-workspace-files" }
