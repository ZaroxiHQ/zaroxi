[package]
name = "zaroxi-core-platform-profiler"
version = "0.1.0"
edition = "2024"
description = "Profiling integrations and adapters."
license = "MIT"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
