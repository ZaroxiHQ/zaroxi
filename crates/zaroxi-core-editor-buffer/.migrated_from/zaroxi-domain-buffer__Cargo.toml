[package]
name = "zaroxi-domain-buffer"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Domain buffer semantics tying domain logic to core text model."
rust-version = "1.85"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
