[package]
name = "zaroxi-domain-workspace"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Workspace model and project metadata (pure logic, no IO)."
rust-version = "1.70"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
