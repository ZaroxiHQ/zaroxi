[package]
name = "zaroxi-domain-workspace"
version = "0.1.0"
edition = "2024"
description = "zaroxi-domain-workspace (auto-generated)"
license = "MIT"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
