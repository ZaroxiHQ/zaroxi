[package]
name = "zaroxi-domain-workspace"
version = "0.1.0"
edition = "2024"
description = "zaroxi-domain-workspace (auto-generated)"
license = "MIT"

[dependencies]
# Minimal domain dependencies used by the Phase 0 DTOs and types.
serde = { workspace = true }
uuid = { workspace = true }
chrono = { workspace = true }
serde_json = { workspace = true }

# Kernel types (strongly-typed ids) used by domain DTOs.
zaroxi-kernel-types = { path = "../zaroxi-kernel-types" }

[lints]
workspace = true
