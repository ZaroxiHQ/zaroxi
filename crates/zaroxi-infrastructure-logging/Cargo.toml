[package]
name = "zaroxi-infrastructure-logging"
version = "0.1.0"
edition = "2024"
description = "zaroxi-infrastructure-logging (auto-generated)"
license = "MIT"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
