[package]
name = "zaroxi-core-editor-rope"
version = "0.1.0"
edition = "2024"
description = "Rope string data structure used by the editor."
license = "MIT"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.

[lints]
workspace = true
