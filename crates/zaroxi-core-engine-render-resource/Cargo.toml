[package]
name = "zaroxi-core-engine-render-resource"
version = "0.1.0"
edition = "2024"
description = "Render resource management (textures, buffers)."
license = "MIT"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.

[lints]
workspace = true
