[package]
name = "zaroxi-kernel-config"
version = "0.1.0"
edition = "2024"
description = "zaroxi-kernel-config (auto-generated)"
license = "MIT"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
