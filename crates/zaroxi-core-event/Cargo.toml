[package]
name = "zaroxi-core-event"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Event bus and typed event propagation primitives."
rust-version = "1.85"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
