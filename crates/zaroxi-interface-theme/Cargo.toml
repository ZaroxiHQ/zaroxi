[package]
name = "zaroxi-interface-theme"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Theme primitives, semantic colors and design tokens used across the UI."

[lib]
name = "zaroxi_interface_theme"
path = "src/lib.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
