[package]
name = "zaroxi-interface-theme"
version = "0.1.0"
edition = "2024"
description = "zaroxi-interface-theme (auto-generated)"
license = "MIT"

[dependencies]
# Serialization helpers used by theme types; use the workspace pins so versions remain centralized.
serde = { workspace = true }
serde_json = { workspace = true }
