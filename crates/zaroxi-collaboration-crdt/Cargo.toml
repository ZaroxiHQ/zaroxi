[package]
name = "zaroxi-collaboration-crdt"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "CRDT implementations for text and presence."
rust-version = "1.85"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
