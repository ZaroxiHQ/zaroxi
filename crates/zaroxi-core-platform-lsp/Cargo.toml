[package]
name = "zaroxi-core-platform-lsp"
version = "0.1.0"
edition = "2024"
description = "Language Server Protocol integration layer."
license = "MIT"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
