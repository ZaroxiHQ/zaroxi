[package]
name = "zaroxi-domain-project"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Project model, dependency graph, and build targets."
rust-version = "1.85"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
