[package]
name = "zaroxi-security-auth"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Authentication flows and token validation (pluggable)."
rust-version = "1.85"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
