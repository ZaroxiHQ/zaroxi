[package]
name = "zaroxi-core-workspace-cache"
version = "0.1.0"
edition = "2024"
description = "Workspace file cache and eviction policies."
license = "MIT"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
