[package]
name = "zaroxi-core-render-resource"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "GPU resource management and lifetime tracking."
rust-version = "1.85"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
