[package]
name = "zaroxi-application-ai"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "High-level AI orchestration: copilots and assistant features."
rust-version = "1.85"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
