[package]
name = "zaroxi-application-ai"
version = "0.1.0"
edition = "2024"
description = "zaroxi-application-ai (auto-generated)"
license = "MIT"

[dependencies]
# Use workspace pinned packages so features (serde, uuid v4, etc.) are correctly used.
tokio = { workspace = true }
tracing = { workspace = true }
anyhow = { workspace = true }
serde = { workspace = true }
uuid = { workspace = true }
serde_json = { workspace = true }

# Domain crate: small DTOs for AI edit requests/proposals (Phase 11).
zaroxi-domain-ai = { workspace = true }

# Kernel types (strongly-typed ids) used by AI request/response DTOs.
zaroxi-kernel-types = { path = "../zaroxi-kernel-types" }
# Core editor buffer canonical BufferId type (core-owned port)
zaroxi-core-editor-buffer = { path = "../zaroxi-core-editor-buffer" }
