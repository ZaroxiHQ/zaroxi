[package]
name = "zaroxi-kernel-traits"
version = "0.1.0"
edition = "2024"
description = "zaroxi-kernel-traits (auto-generated)"
license = "MIT"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
