[package]
name = "zaroxi-kernel-traits"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Minimal shared trait definitions used across layers."
rust-version = "1.70"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
