[package]
name = "zaroxi-core-render-backend"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Backend adapters (wgpu/vulkan/metal) behind feature flags."
rust-version = "1.70"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
