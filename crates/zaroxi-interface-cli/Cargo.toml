[package]
name = "zaroxi-interface-cli"
version = "0.1.0"
edition = "2024"
description = "zaroxi-interface-cli (auto-generated)"
license = "MIT"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
