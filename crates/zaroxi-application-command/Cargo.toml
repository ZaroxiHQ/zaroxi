[package]
name = "zaroxi-application-command"
version = "0.1.0"
edition = "2024"
description = "zaroxi-application-command (auto-generated)"
license = "MIT"

[dependencies]
# Workspace-shared small dependencies
serde = { workspace = true }
uuid = { workspace = true }
chrono = { workspace = true }

# Core canonical BufferId and kernel Id types used by command DTOs
zaroxi-core-editor-buffer = { path = "../zaroxi-core-editor-buffer" }
zaroxi-kernel-types = { path = "../zaroxi-kernel-types" }

[lints]
workspace = true
