[package]
name = "zaroxi-application-command"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Command registry glue and permission checks."
rust-version = "1.85"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
