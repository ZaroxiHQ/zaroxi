[package]
name = "zaroxi-application-refactor"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Refactoring orchestration and safe apply mechanisms."
rust-version = "1.85"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
