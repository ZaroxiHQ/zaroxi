[package]
name = "zaroxi-interface-app"
version = "0.1.0"
edition = "2024"
description = "zaroxi-interface-app (Phase 48 minimal)"
license = "MIT"

[dependencies]
# Engine-facing semantic input type (non-visual). Interface depends on core (downward).
zaroxi-core-engine-view = { path = "../zaroxi-core-engine-view" }

# Render crate dependency: expose the canonical ShellDrawPlan / ShellRenderTranscript
# types produced by the rendering subsystem so this application-facing crate can
# convert plans into tiny UI-facing view models.
zaroxi-core-engine-render = { path = "../zaroxi-core-engine-render" }

[dev-dependencies]
# Tests in this crate import the desktop presenter/projections for integration-style
# checks. Add a dev-dependency so test code can reference the desktop crate without
# introducing an application -> interface dependency in normal builds.
zaroxi-interface-desktop = { path = "../zaroxi-interface-desktop" }
