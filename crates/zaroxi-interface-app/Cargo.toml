[package]
name = "zaroxi-interface-app"
version = "0.1.0"
edition = "2024"
description = "zaroxi-interface-app (Phase 48 minimal)"
license = "MIT"

[dependencies]
# Minimal, explicit path dependency to the desktop projection crate used by
# ShellFrameViewModel. Using a path dependency avoids inheriting missing
# workspace pins and keeps this crate self-contained during phased migration.
zaroxi-interface-desktop = { path = "../zaroxi-interface-desktop" }
