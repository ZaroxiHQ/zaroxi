[package]
name = "zaroxi-interface-app"
version = "0.1.0"
edition = "2024"
description = "zaroxi-interface-app (auto-generated)"
license = "MIT"

[dependencies]
serde = { workspace = true }
# `log` is not declared in the workspace dependencies; depend on a specific compatible
# version here to avoid inheriting an absent workspace dependency.
log = "0.4"

# Interface and workspace-local crates consumed by this crate.
zaroxi-interface-desktop = { workspace = true }
# The workspace does not declare a workspace-level pin for `zaroxi-config`, avoid inheriting
# a missing workspace dependency by pinning a local version. If the actual package is a
# workspace member under a different path/name, convert this entry to a path = "../..." form.
zaroxi-config = "0.1.0"
zaroxi-editor-core = { workspace = true }
zaroxi-editor-buffer = { workspace = true }
zaroxi-workspace = { workspace = true }
zaroxi-theme = { workspace = true }
zaroxi-foundation = { workspace = true }

# Validation hint (comment): run `cargo test -p zaroxi-interface-app` to confirm build.
