[package]
name = "zaroxi-interface-app"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Desktop/web app shell and orchestration (entrypoint)."
rust-version = "1.70"

[dependencies]
# Add crate-specific dependencies when replacing stubbed manifests.
