# Clippy lint configuration for the Zaroxi workspace.
# Only contains clippy configuration keys — not workspace manifest data.

# Relax some pedantic lints that are acceptable for this codebase.
allow-unwrap-in-tests = true
too-many-arguments-threshold = 10
