[package]
name = "tools-generate-stubs"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Workspace helper that creates missing crate stubs for members listed in Cargo.toml."

[[bin]]
name = "generate-stubs"
path = "src/main.rs"
